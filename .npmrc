# openai's optional zod@^3 peer conflicts with @anthropic-ai/sdk's zod@^4 --
# both are satisfied at runtime, npm's strict resolver just can't tell.
# Without this, `npm install` (including Replit's deploy build) fails with
# ERESOLVE.
legacy-peer-deps=true
