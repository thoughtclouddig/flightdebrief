Replace Replit Auth (OIDC "Log in with Replit") with email magic-link sign-in. Students/CFIs must never be asked to have a Replit.com account — the current app/api/auth/login/route.ts + app/api/auth/callback/route.ts (OIDC/PKCE via lib/auth/replit.ts) need to go entirely, replaced with:

POST /api/auth/login — accepts { email }. Generate a short-lived (~15 min) signed JWT containing the normalized (lowercased, trimmed) email as a "magic-link" purpose claim — a new helper in lib/auth/session.ts alongside the existing session JWT (keep it a separate signing purpose, not reused for the actual session token). Email a link like ${appOrigin}/api/auth/callback?token=... via a new sendMagicLinkEmail() in lib/email.ts, following the same pattern as sendInviteEmail() (escaped HTML, appOrigin() for the link, best-effort send). Always respond success regardless of whether the email exists, to avoid leaking which emails have accounts. Add basic rate-limiting (e.g. a per-email cooldown) so it can't be used to spam arbitrary inboxes.
GET /api/auth/callback?token=... — verify the magic-link token, extract the email. Call resolveUserOnLogin() in lib/auth/store.ts unchanged — pass sub: normalizedEmail (the email itself becomes the stable identity anchor instead of a Replit OIDC sub), email, name: null. This keeps the owner-bootstrap / invite-linking / not-invited-rejection logic exactly as already built and tested — do not touch resolveUserOnLogin(). On success, issue the existing session JWT (createSessionJwt) exactly as today. On invalid/expired token or not-invited, redirect to /login?error=... reusing the existing error query params the login page already handles.
First-login profile completion. When resolveUserOnLogin() links an invited user's account on their first successful magic-link login (the "invited but not yet linked" branch), don't drop them straight into /app. Instead redirect to a short one-time form (e.g. /onboarding) gated by a new boolean like users.profile_completed (default false, set true on submit) where they confirm/edit their name (pre-filled from what the admin entered at invite time) before continuing to /app. Returning users who already completed it skip straight through. The owner-bootstrap first-login case goes through the same completion step, not just invited users.
app/(auth)/login/page.tsx — replace the "Log in with Replit" button with an email input + submit, POSTing to /api/auth/login, then showing a "check your email" confirmation state.
Delete: lib/auth/replit.ts, the openid-client dependency, REPL_ID/ISSUER_URL env var usage. lib/auth/session.ts's existing session logic, lib/auth/guard.ts, lib/auth/access.ts, lib/viewer.ts, proxy.ts, and every ownership check stay exactly as-is — they only depend on the session cookie, not on how it was established.
e2e suite: e2e/mock-oidc.mjs and the OIDC parts of e2e/auth-journey.mjs need reworking since there's no OIDC flow to mock anymore — the simplest approach is having the test suite mint magic-link tokens directly (same signing function the route uses) instead of spinning up a fake OIDC issuer. Keep the same scenarios (owner bootstrap, invite+link, not-invited rejection, logout, deactivation) and add coverage for the new profile-completion step.
Verify end-to-end with a real magic-link email sent through Resend before reporting done, same bar as the invite-email work.